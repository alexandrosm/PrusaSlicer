; Unknown option names must not prevent valid neighboring settings from loading.
; This is valid INI syntax, not a test of syntactically malformed INI values.
unknown_fixture_option_before = ignored
layer_height = 0.25
perimeters = 3
unknown_fixture_option_after = also ignored
